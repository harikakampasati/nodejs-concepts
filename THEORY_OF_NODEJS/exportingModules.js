/**
 * a module is another script file, from where we can export some values and then those exported values can be used in other script files by simply importing it.
 * each script file in node js a module 
 * module are 3 types:
 * 1 core modules --> provided by node js
 * 2 custom modules (user define modules) --> these modules create or defined by the developers 
 * 3 third-party modules or libraries  
 * 
 * custom modules
 * to export these module we use module object and exports keyword 
 */